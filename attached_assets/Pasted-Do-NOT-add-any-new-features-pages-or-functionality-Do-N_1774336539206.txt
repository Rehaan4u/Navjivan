Do NOT add any new features, pages, or functionality.
Do NOT change any logic, routes, authentication, or backend code.
ONLY redesign the frontend CSS/UI of the existing website.
Every element that exists stays — just make it look significantly 
more premium, formal, and visually refined.

=============================================================
CURRENT SITE ANALYSIS — WHAT NEEDS TO CHANGE
=============================================================

The site currently uses a flat royal blue (#2563eb) as the dominant 
color with basic card layouts. It looks like a default Tailwind/Bootstrap 
project. The goal is to elevate it to feel like a premium fintech 
publication dashboard — think Bloomberg Terminal meets Stripe Dashboard.

=============================================================
COLOR SYSTEM — REPLACE ENTIRELY
=============================================================

:root {
  /* Core backgrounds */
  --bg-primary      : #0a0f1e;   /* Deep navy — main page background */
  --bg-surface      : #111827;   /* Card/panel backgrounds */
  --bg-surface-2    : #1a2235;   /* Slightly elevated cards (hover states) */
  --bg-banner       : linear-gradient(135deg, #0f2444 0%, #1a3a6b 50%, #0f3460 100%);

  /* Accent colors */
  --accent-gold     : #c9a84c;   /* Primary accent — buttons, highlights */
  --accent-gold-soft: rgba(201, 168, 76, 0.12); /* Gold tint for hover bg */
  --accent-teal     : #0f9d8f;   /* Secondary accent — active states, badges */
  --accent-teal-soft: rgba(15, 157, 143, 0.12);

  /* Text */
  --text-primary    : #f0ece2;   /* Main text — warm off-white */
  --text-secondary  : #9ca3af;   /* Muted text — descriptions, captions */
  --text-gold       : #c9a84c;   /* Gold text for labels, eyebrows */

  /* Borders */
  --border-subtle   : rgba(255,255,255,0.06);
  --border-gold     : rgba(201, 168, 76, 0.25);
}

Apply these to: body background, all cards, navbar, banner, buttons.

=============================================================
TYPOGRAPHY UPGRADE
=============================================================

Load from Google Fonts (add to <head>):
  Playfair Display — for "Payment Chronicle" brand name only
  Inter — for all other UI text

- Brand name "Payment Chronicle" in navbar:
    font-family: 'Playfair Display', serif
    font-size: 22px
    font-weight: 700
    color: var(--accent-gold)
    letter-spacing: -0.01em

- Add below brand name (same navbar):
    "by Gajanan Pujari"
    font-family: 'Inter', sans-serif
    font-size: 10px
    font-weight: 400
    letter-spacing: 0.15em
    text-transform: uppercase
    color: var(--text-secondary)

- All other text: Inter, with proper weight hierarchy:
    Page titles (Welcome back): 28px, weight 600, --text-primary
    Section titles (Company Preferences etc): 16px, weight 600, --text-primary
    Descriptions: 14px, weight 400, --text-secondary
    Labels/eyebrows: 11px, weight 500, uppercase, letter-spacing 0.1em

=============================================================
NAVBAR REDESIGN
=============================================================

- Background: rgba(10, 15, 30, 0.95) with backdrop-filter: blur(16px)
- Bottom border: 1px solid var(--border-subtle)
- Height: 64px
- "Home" active tab: 
    Remove the current pill/box style
    Instead: gold bottom border (2px solid var(--accent-gold))
    Text color: var(--accent-gold)
    Background: transparent
- "Newsletter Archive" tab:
    Text: var(--text-secondary)
    Hover: color becomes var(--text-primary)
    Badge "14": 
      background: var(--accent-gold)
      color: #0a0f1e
      border-radius: 999px
      padding: 2px 7px
      font-size: 11px
      font-weight: 600
- User section (Rehaan + Log out):
    Avatar icon: gold tinted background circle
    "Rehaan": var(--text-primary), Inter 14px
    "Log out": var(--text-secondary), hover → var(--accent-gold)
    Separator: 1px vertical line var(--border-subtle) between them

=============================================================
HERO BANNER REDESIGN
=============================================================

Replace the flat blue banner with:
- Background: var(--bg-banner) gradient
- Add a very subtle dot-grid pattern overlay using CSS:
    background-image: radial-gradient(circle, rgba(201,168,76,0.08) 1px, transparent 1px)
    background-size: 24px 24px
- Left side content:
    "Good to see you" → 
      font-size: 12px, uppercase, letter-spacing 0.15em
      color: var(--accent-gold)
      margin-bottom: 8px
    "Welcome back, Rehaan" →
      Playfair Display, 32px, weight 700, color: var(--text-primary)
    Subtitle text →
      Inter 15px, var(--text-secondary), line-height 1.6
- Right side cards (Subscription Active + Next Delivery):
    Background: rgba(255,255,255,0.06)
    Border: 1px solid var(--border-gold)
    Border-radius: 10px
    Backdrop-filter: blur(8px)
    "Active" badge: 
      color: #10b981 (green)
      background: rgba(16,185,129,0.12)
      border-radius: 4px
      padding: 2px 8px
      font-size: 12px font-weight 600
    "9:00 AM IST daily":
      font-size: 16px, font-weight: 600, color: var(--text-primary)
    Icon circles: var(--accent-gold) tint background

=============================================================
CARDS REDESIGN (Company Preferences + Quick Actions)
=============================================================

All cards:
- Background: var(--bg-surface)
- Border: 1px solid var(--border-subtle)
- Border-radius: 16px
- Padding: 28px 32px
- Box-shadow: 0 4px 24px rgba(0,0,0,0.3)
- On hover: border-color transitions to var(--border-gold)
            box-shadow lifts: 0 8px 40px rgba(0,0,0,0.4)
            transition: all 0.25s ease

Section header (icon + title + description):
- Icon container: 
    width/height: 40px
    background: var(--accent-gold-soft)
    border-radius: 10px
    border: 1px solid var(--border-gold)
    icon color: var(--accent-gold)
- Title: Inter 16px, weight 600, var(--text-primary)
- Description: Inter 13px, var(--text-secondary)

Company Preferences card:
- Tag chip "Paypal ×":
    background: var(--accent-gold-soft)
    border: 1px solid var(--border-gold)
    color: var(--accent-gold)
    border-radius: 6px
    padding: 5px 12px
    font-size: 13px
    × button: var(--text-secondary), hover → var(--accent-gold)
- Input area:
    background: var(--bg-primary)
    border: 1px solid var(--border-subtle)
    border-radius: 8px
    color: var(--text-primary)
    focus: border-color var(--accent-gold), 
           box-shadow: 0 0 0 3px rgba(201,168,76,0.1)
- "Update Preferences" button:
    background: var(--accent-gold)
    color: #0a0f1e
    font-weight: 600
    font-size: 14px
    border-radius: 8px
    padding: 10px 20px
    border: none
    hover: brightness(1.1), scale(1.02)
    transition: all 0.2s ease

Quick Actions card:
- Each action item:
    background: var(--bg-surface-2)
    border: 1px solid var(--border-subtle)
    border-radius: 10px
    padding: 16px 20px
    display: flex, align-items: center, gap: 16px
    margin-bottom: 10px
    cursor: pointer
    hover: 
      background: var(--bg-surface)
      border-color: var(--border-gold)
      transform: translateX(4px)
      transition: all 0.2s ease
- "Generate Newsletter" item icon:
    background: var(--accent-gold)
    color: #0a0f1e
    (this is the primary action, make it stand out)
- Other icons:
    background: var(--accent-gold-soft)
    color: var(--accent-gold)
- Item titles: Inter 14px, weight 500, var(--text-primary)
- Item descriptions: Inter 12px, var(--text-secondary)

=============================================================
RECENT NEWSLETTERS SECTION
=============================================================

- Section header:
    "Recent Newsletters": Inter 16px, weight 600, var(--text-primary)
    "Your latest briefings": Inter 13px, var(--text-secondary)
    "View all 14 →": var(--accent-gold), hover: brightness(1.2)
    Icon: same style as other section icons (gold tint)
- Newsletter list items (cards/rows):
    Same card treatment as above
    On hover: translateX(4px) + gold border glow
    Date/time labels: var(--text-secondary), 12px
    Title text: var(--text-primary), 14px, weight 500

=============================================================
GLOBAL POLISH
=============================================================

- Page background: var(--bg-primary) — apply to body and all wrappers
- All scrollbars: 
    width: 6px
    track: transparent  
    thumb: rgba(201,168,76,0.3)
    thumb hover: rgba(201,168,76,0.6)
- Page max-width container: 1200px, centered, padding: 0 24px
- Section spacing: 24px gap between all major cards/sections
- All transitions default: 0.2s ease unless specified otherwise
- Remove any box-shadows that use blue tints — replace with black-based shadows

=============================================================
DO NOT TOUCH
=============================================================

- No changes to routes, API calls, authentication logic
- No changes to data fetching or state management  
- No new sections, components, or features added
- No changes to how the newsletter archive page works
- Preserve all existing class names and IDs that JS depends on —
  only change visual CSS properties
- If using Tailwind: override via a custom CSS file, do not 
  restructure component JSX beyond adding/changing class names